<UserControl x:Class="HypervisorOverlay.Views.HOverlayControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="50">
    <Grid>
        <Border x:Name="BadgeBorder" 
                Background="#CC990000" 
                CornerRadius="12" 
                Width="24" Height="24" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center" 
                ToolTip="Requer Virtualização (Hypervisor)">
            <Border.Effect>
                <DropShadowEffect BlurRadius="5" ShadowDepth="0" Color="Red" Opacity="0.5"/>
            </Border.Effect>
            <TextBlock Text="H" 
                       Foreground="White" 
                       FontSize="15" 
                       FontWeight="ExtraBold" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       FontFamily="Segoe UI Semibold">
                <TextBlock.Effect>
                    <DropShadowEffect BlurRadius="2" ShadowDepth="1" Color="Black" Opacity="0.5"/>
                </TextBlock.Effect>
            </TextBlock>
        </Border>
    </Grid>
</UserControl>
