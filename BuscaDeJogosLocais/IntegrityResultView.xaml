<UserControl x:Class="BuscaDeJogosLocais.IntegrityResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Os seguintes jogos possuem arquivos ou pastas ausentes:" Margin="0,0,0,10" FontWeight="Bold" Foreground="White" />

        <ListView Grid.Row="1" ItemsSource="{Binding Results}" MaxHeight="600" Width="700" Background="#222" Foreground="White">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Remover?" Width="70">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding Selected}" HorizontalAlignment="Center" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Jogo" DisplayMemberBinding="{Binding Name}" Width="200" />
                    <GridViewColumn Header="Status" DisplayMemberBinding="{Binding StatusSummary}" Width="150" />
                    <GridViewColumn Header="Caminho" DisplayMemberBinding="{Binding InstallDir}" Width="260" />
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Executar Ação" Click="Confirm_Click" Width="150" Height="30" Margin="0,0,10,0" />
            <Button Content="Fechar" Click="Cancel_Click" Width="100" Height="30" />
        </StackPanel>
    </Grid>
</UserControl>
